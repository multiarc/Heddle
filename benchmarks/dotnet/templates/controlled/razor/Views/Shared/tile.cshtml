@*
    Razor parity twin -- fragment-heavy tile partial (ledger E20).

    One of the four dispatched fragment kinds. Html.Raw: raw-suite non-encoding path.
*@
@using Heddle.Benchmarks.Dotnet.Models
@model FragmentContent.FragmentRow
<section class="tile"><h3>@Html.Raw(Model.Name)</h3><p class="v">@Model.Value</p><span class="badge">@Html.Raw(Model.Badge)</span></section>
