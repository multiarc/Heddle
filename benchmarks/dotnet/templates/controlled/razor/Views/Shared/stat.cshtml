@*
    Razor parity twin -- fragment-heavy stat partial (ledger E20).

    One of the four dispatched fragment kinds. Html.Raw: raw-suite non-encoding path.
*@
@using Heddle.Benchmarks.Dotnet.Models
@model FragmentContent.FragmentRow
<div class="stat"><span class="stat-name">@Html.Raw(Model.Name)</span><span class="stat-value">@Model.Value</span><span class="stat-delta">@Model.Delta</span></div>
