@*
    Razor parity twin -- fragment-heavy badge sub-partial (ledger E20).

    Nested by the card partial against the row's Promo. Html.Raw: raw-suite non-encoding path.
*@
@using Heddle.Benchmarks.Dotnet.Models
@model FragmentContent.FragmentPromo
<span class="promo-badge">@Html.Raw(Model.Label)</span>
