# Cargo build artifacts (Cargo.lock IS committed — a reproducibility pin).
/target
# alloc_report's per-run measurement output. The master runners copy it into the
# run directory, and the published copy lives there — never here.
/alloc-report.txt
