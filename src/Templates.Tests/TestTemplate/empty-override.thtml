@using(){{Templates.Tests.Data}}
@using(){{System.Collections.Generic}}
@model(){{dynamic}}
<%
      
<category1>
{{
	@(ComplexObject.Data.Text)
    @if(@model.SubCategories.Count>0)
    {{
        <ul class="drop-menu sub-menu collapsed">
        @list(SubCategories)
        {{
            <li>
                @if(@model.SubCategories.Count>0)
                {{
                <a class="trigger" href="#">@(Name)</a>
                }}
                @if(@model.SubCategories.Count==0)
                {{
                <a href="@(Name)">@(Name)</a>
                }}
				@(@root.Count) C# Test
				@(::Count) Model Access Test
            </li>
        }}
        </ul>
    }}
}} :: Category

<category>
{{
	@(ComplexObject.Data.Text)
    @if(@model.SubCategories.Count>0)
    {{
        <ul class="drop-menu sub-menu collapsed">
        @list(SubCategories)
        {{
            <li>
                @if(@model.SubCategories.Count>0)
                {{
                <a class="trigger" href="#">@(Name)</a>
                }}
                @if(@model.SubCategories.Count==0)
                {{
                <a href="@(Name)">@(Name)</a>
                }}
				@(@root.Count) C# Test
				@(::Count) Model Access Test
            </li>
        }}
        </ul>
    }}
}} :: Category

<left> -> ()
{{
 <div class="left-content-pane">
     <strong>ARTICLES BY TOPIC</strong>
        <br/>
        <br/>
        <ul class="drop-menu">
            @list()
            {{
                <li>
                    <a class="trigger" href="#">@(Name)</a>
                    @category()
					@category1()
                </li>
            }}
        </ul>
 </div>
}} :: ICollection<Category>

<category:category>
{{}}

<category1:category1>
{{}}

%>