# Historical records

The append-only record of completed spec work. The [common specs](common/spec-conventions.md)
are instruction-only — anything that documents *finished* work (per-window as-shipped
records, the accumulated amendments ledger, registry corrections) lives here instead.
Entries are never edited or deleted; later facts are recorded as new entries.

## The 2.0 breaking window — as-shipped record

The first window under the [breaking-windows policy](common/breaking-windows.md).
**Window status: closed** — `v2.0.0` released 2026-07-19; contents reconciled against the
shipped source per policy rule 5. The migration note shipped as the release's
[CHANGELOG entry](../../CHANGELOG.md) (policy rule 4).

**Window contents as shipped in `v2.0.0`:**

| # | Change | 1.x state | As shipped in 2.0.0 |
| --- | --- | --- | --- |
| 1 | `OutputProfile` default flip | `Text` | ✅ `Html` — the unnamed `@(...)` encodes by default ([TemplateOptions.cs](../../src/Heddle/Data/TemplateOptions.cs) ctor) |
| 2 | Encoder pluggability | `WebUtility.HtmlEncode` at the pinned call sites; no seam | ✅ `TemplateOptions.Encoder` (`System.Text.Encodings.Web.TextEncoder` seam with span/UTF-8 paths, [HtmlEncodedRenderer.cs](../../src/Heddle/Data/HtmlEncodedRenderer.cs)); the **default (`null`) keeps the 1.x `WebUtility.HtmlEncode` bytes**. The byte-changing *default swap* to `HtmlEncoder.Create(UnicodeRanges.All)` is **not** in this window — deferred to the [next-window candidate register](common/breaking-windows.md#next-window-candidate-register) until the `Encoder` pin has soaked |
| 3 | `TrimDirectiveLines` default flip | `false` | ✅ `true` |
| 4 | `[Obsolete]` on `AllowCSharp` | Un-attributed bridge over `ExpressionMode` | ✅ `[Obsolete]`, behavior unchanged (compatibility bridge keeps working) |
| 5 | Generator MSBuild defaults follow the engine | `HeddleOutputProfile=Text`, `HeddleTrimDirectiveLines=false` | ✅ `Html` / `true` ([Heddle.Generator.props](../../src/Heddle.Generator/build/Heddle.Generator.props)) |
| 6 | Legacy `@import()` removal | Functional include | ✅ Removed — a call site fails with a single positioned `HED4003` **error** naming both replacements (`@<<{{ path }}` / `@partial(){{ name }}`); the `import` name is kept as a registered no-op tombstone so the diagnostic is a targeted migration signpost. See [language-reference.md](../language-reference.md#imports---) |
| 7 | `ForModel` → `Range` remodel | `Heddle.Models.ForModel` (mutable class, nullable members) | ✅ `Heddle.Models.ForModel` deleted; `range(...)`, `PrecompiledFunctions.Range`, and `@for` remodeled onto the immutable `Heddle.Models.Range` value type (`public readonly struct`). Only rendered-byte effect: range stringification now renders the readable call form (`range(2, 10, 2)`) instead of the type name |

**Also part of the window — precompiled assemblies from 1.x fall back under a 2.0
engine.** No compatibility shim: the engine-version gate rejects 1.x manifests; outcome
follows `PrecompiledMismatchPolicy` (`Fallback` recompiles with `HED7101`, `Strict`
throws). Documented in the [CHANGELOG](../../CHANGELOG.md) and
[precompilation.md](../precompilation.md). Rationale preserved: the gate exists so a
byte-changing window can never be masked by stale generated code.

> **Appended correction, 2026-07-26 (this record is append-only, so the paragraph above stands as
> ratified and this note supersedes it).** The paragraph describes a case that cannot occur.
> **Pre-compilation shipped in 2.0.0**, so no 1.x manifest has ever existed, no gate has ever fired
> for one, and there is no population of "precompiled assemblies from 1.x" to fall back. What is true
> is the *rationale* — the engine-version and schema gates exist so stale generated code cannot mask a
> byte-changing window — and that mechanism did ship and does fire, for manifests built by an
> incompatible **2.x** generator. Why the claim survived: it was reasoned from the gate's existence
> rather than from the release history, the same failure mode policy rule 5 exists to catch, and it
> propagated into the CHANGELOG's Compatibility section (corrected there directly, that file being
> editable). Recorded rather than deleted so the reasoning error is visible, not just its result.

**Explicitly excluded from the 2.0 window:** the default-encoder *swap* (item 2 — a
[next-window candidate](common/breaking-windows.md#next-window-candidate-register) gated
on the shipped `Encoder` pin soaking); `[NotEncode]` type deletion (needs its own
ratified window — a next-window candidate); `TypeForwardedTo` hygiene for
`Heddle.LanguageServices` (churn without benefit); async render APIs (sync-sinks decision
stands); any grammar change beyond the `@import` tombstone (everything else shipped
additively at the grammar level).

**Verification gate (as executed):** the standard regression gate plus
`samples/html-safe-output` running under both profile defaults, and the precompiled ==
dynamic differential against the 2.0 defaults proving engine and generator flipped
together.

## The 2.1 breaking window — as-shipped record

**2.1 is a ratified breaking window** (maintainer ruling, 2026-07-26), scoped to **binary changes and
minor API changes or additions**. A *substantial* API change is what would call for 3.0, and that is
explicitly not on the table; anything of that size is refused entry to this window rather than being
argued into it.

**This paragraph previously read "2.1 opens no window"**, on the reasoning that each item was defect
repair and policy rule 1 ("one window per major") therefore stayed untouched. That was superseded: a
window is opened by an explicit maintainer decision, not by which version component carries it, and rule
1's wording is what gives way. The individual not-window-gated dispositions in
[breaking-windows.md](common/breaking-windows.md#explicit-not-window-gated-rulings) remain accurate about
*why each change is safe* — they simply no longer have to carry the weight of keeping the release
window-less.

Policy rule 5's as-shipped reconciliation still governs: the window ships a binary break in the
precompiled-manifest contract, and reconciliation is the only mechanism that keeps such a thing from being
assumed rather than verified.

**A correction, recorded here because it is exactly what that mechanism is for.** This paragraph previously
read that the break "had already occurred in 2.0.0's metadata". It had not. Checked against the `v2.0.0`
tag: the shipped generator emitted `schemaVersion: 2`, the shipped engine accepted `1–2`, and
`PrecompiledExtensionBinding` still had the real two-argument constructor. The break is **pending** and lands
in 2.1 — see row 1. The claim survived several documents because it was reasoned from the current source
rather than from the release, which is the failure mode rule 5 exists to catch.

**Window status: open and ratified** (binary + minor API scope). **Release status: pending** — recorded at implementation time;
reconcile against the shipped source when `v2.1.0` is tagged.

| # | Change | 2.0.0 state | As implemented for 2.1.0 |
| --- | --- | --- | --- |
| 1 | Precompiled manifest schema floor and window (Q8.2, **corrected**) | `MinSupportedSchemaVersion = 1`, `Max = 2`, and the generator emitted `schemaVersion: 2` — **verified against the `v2.0.0` tag**, where `PrecompiledSchema.cs` did not yet exist and `PrecompiledExtensionBinding` still had a real two-argument constructor that the shipped emitter called. Schemas 1 and 2 are the only schemas that have ever shipped | ✅ `Min = Max = Current = 3` ([PrecompiledSchema.cs](../../src/Heddle/Precompiled/PrecompiledSchema.cs)). Two parts. **(a) The floor rises 1 → 3**, excluding exactly the released set: 2.1 removes the two-argument constructor (the prop-layout fingerprint lands as an optional third parameter), so a schema 1–2 manifest's IL names a member that no longer exists, and a floor of `1` would accept it and then fault with `MissingMethodException` out of `PrecompiledTemplates.Register`. A 2.0.x-precompiled assembly now degrades cleanly with one `HED7102` (`SchemaVersionUnsupported`) callback, or throws under `PrecompiledMismatchPolicy.Strict`. No compatibility shim, by ruling. **(b) The three unreleased increments collapse into one**: what had been 3 (dynamic-member routing), 4 (prop-layout row) and 5 (per-carrier `BindDefinition`) are all schema 3, which also carries Q8.30's `RegisteredName` and Q8.31's `LinePathForm` — an increment no user could observe is not a migration step, and three would advertise a history that never existed. **The superseded record's reasoning was false and is withdrawn, not softened:** it claimed the constructor break "already shipped in 2.0.0 because schema 4's optional parameter removed the ctor then". Schema 4 never shipped; the break is pending and lands here. Demonstrated by `OldSchemaManifestRejectionTests` over both released schemas, against a reference facade under the real assembly's identity — the superseded pin constructed its binding through the optional parameter and therefore exercised a *new*-schema call |
| 2 | `<HeddleTemplate>` per-item metadata (Q8.12) | `Key`, `Name` and `Precompile` all inert from a real project: the targets restated each inside an `Include="@(HeddleTemplate)"` transform, and outside a target a cross-item `%()` reference evaluates to `""`, so each element overwrote the value the transform had copied. `Name` had additionally been *deleted* from the props by phase 5, on a review record that overreached its ask | ✅ All three effective ([Heddle.Generator.targets](../../src/Heddle.Generator/build/Heddle.Generator.targets)). `Name` restored — first as a second spelling of `Key`, i.e. an override, which broke path imports and was **corrected under Q8.25** to be **additive**: the template keeps its key and gains the name, both spellings resolve, and importing a named template by its key warns (`HED7028`) instead of failing. `Name` therefore takes no part in `HED7002`/`HED7003` and does not suppress `HED7018`; `HED7004` keeps a `Name` arm for an unusable or already-taken name. `samples/codegen-t4-successor` demonstrates it with a named import-only partial; its rendered output is unchanged. **Scope moved a third time under Q8.30**: the manifest row now carries the name and the runtime registry answers to it, keys first, so `Name` is a lookup spelling at both tiers rather than at build time only — see row 6 |
| 3 | Generated `#line` file (Q8.12, fallout) | The registration key, indistinguishable from the file path while every key was path-derived | ✅ The template's root-relative path under `HeddleTemplateRoot`, its own (absolute) `AdditionalText.Path` outside it (Q8.27 — the old fallback was a bare filename no compiler could open). Only an explicit `Key` can move it now that `Name` is additive. **Which of the two forms a template uses is manifest data** (`PrecompiledTemplateInfo.LinePathForm`, Q8.31), not the header comment Q8.27 first emitted — a comment is unreadable to the symbolizers and editors that would want it. The comment is deleted; eight Verify snapshots and the sample's generated-source golden move by that line, and the sample's rendered output does not |
| 4 | Release-line statements (Q8.11) | Nine per-project `<Version>` elements (four on non-shipping projects), plus four npm manifests, `PINNED_VERSION` in the VS Code extension, an `lsp.yml` `--version`, four prose sentences, and a `1.0.0` const in the language server — eighteen statements, no lockstep test | ✅ One `<VersionPrefix>` in [Directory.Build.props](../../Directory.Build.props); the un-centralisable statements gated by `VersionConsistencyTests`; the language server's version derived from its assembly. The CI beta job's `--version-suffix` lost a leading dash that a composed `VersionPrefix` can no longer carry |
| 5 | Strong naming (Q8.11) | `Heddle.Demo.Models` and `Heddle.Demo.Wasm` unsigned, drawing `CS8002` in the signed `Heddle.LanguageServices.Tests` | ✅ Both signed with `heddle.snk`. The remaining unsigned reference is third-party (Scriban) and is accepted at the reference through a declared list in `Directory.Build.targets`, not by a project-level `NoWarn` |
| 6 | Registered `Name` as a runtime lookup spelling (Q8.30) | The manifest carried keys only; a `Name` reached the build-time `@<<` import map and stopped there, so nothing at run time could resolve by it | ✅ `PrecompiledTemplateInfo.RegisteredName` on the row, and a second index in [PrecompiledTemplates](../../src/Heddle/Precompiled/PrecompiledTemplates.cs) consulted **after** keys. **Keys win**, independently of registration order, enforced as a disjointness invariant from both directions (a name whose spelling a key owns is refused; a key arriving later evicts the name shadowing its spelling) — a name is an addition, and an addition that displaced an existing spelling would be the override Q8.25 corrected. The cross-assembly collision is the new `HED7104` / `RegisteredNameUnavailable`, reported through `OnFallback` and never a throw, because a broken addition costs the addition and nothing more; a duplicate **key** still throws, and the two are contrasted in one test so neither drifts into the other |
| 7 | Opted-out items validated and advised (Q8.28 / Q8.29) | The diagnostics loop `continue`d on `!Precompile` **before** key derivation, so a malformed `Key`, a malformed `Name` or an already-taken `Name` on an import-only item produced no diagnostic at all — the name silently failed to register and every `@<<` that used it drew `HED7011` at the *importer*. `HED7028` likewise could not fire for an import inside an opted-out file, the case it is most for | ✅ Key and name derivation moved ahead of the gate: an opted-out item raises the same `HED7004` faults an included one does, and is parsed in advisory-only mode so its own imports draw `HED7028`. `HED7029` was **not needed** and stays free — every fault had an existing home, and the one genuinely new collision class is cross-assembly and therefore runtime (`HED7104`, row 6). The opt-out's contract is unchanged and pinned: no entry point, no manifest entry. Its *template* errors stay unreported by decision, since the file is excluded from this build by request; they surface through any precompiled template that imports it |

| 8 | `PrecompiledFallbackEvent` carriers (Q8.33) | One `Key` property holding two different kinds of string — a template key for per-request reasons, an assembly name for the registration-time ones — with no discriminator, so a host had to re-derive from `Reason` which of the two it had. The convention was stated in the type's XML doc and **nowhere in [precompilation.md](../precompilation.md)**, which never mentioned the overload at all | ✅ `Key` **removed** and replaced by `TemplateKey` + `AssemblyName`, exactly one populated, constructed through `ForTemplate`/`ForAssembly` which each refuse the other carrier's reasons. Removal rather than narrowing, deliberately: narrowing leaves a 2.0 host silently reading `null` on the channel whose whole purpose is that failures are not silent, while removal is a compile error at the one line that must change. The reason → carrier mapping is enforced by an exhaustive classifier that **throws on an unclassified reason**, so a reason added later is unraisable until it is mapped. `precompilation.md` now carries the carrier table |
| 9 | Unnormalizable manifest `Name` (Q8.32) | Registration `continue`d past a `RegisteredName` the shared key rule refuses — a `..` segment, a trailing separator, whitespace — with **no event at all**, the one wholly silent drop in the registration path while both collision arms reported | ✅ Reported as `HED7104` / `RegisteredNameUnavailable`, naming the refused spelling and the requesting key. **No new id**: the registry row and the enum member were widened, because from the host's side the two causes are one situation with one remedy — a name it expected does not resolve, and the template is still reachable by its key. The population is manifests no build tier vetted, since the generator emits only names that registered at build time. A per-request gauntlet arm for `RegisteredName` was **rejected** (Q8.32 ruling): the name resolves to an entry whose every row the gauntlet already validates, so re-checking it validates nothing |

| 10 | Assembly auto-loading and scan-all extension discovery (Q8.37) | `AssemblyHelper`'s **static constructor** `Assembly.Load`d the entry assembly's whole transitive `GetReferencedAssemblies()` closure plus every `DependencyContext.GetDefaultAssemblyNames()` entry — unconditionally, at type init, with `FileNotFoundException`, `FileLoadException`, `ReflectionTypeLoadException` and `BadImageFormatException` all swallowed — and `TemplateFactory`'s static constructor scanned that set for `[ExportExtensions]`. Because the scanned set decides extension **name ownership**, an assembly the integration layer never chose to load could take a name, or collide with an unrelated claimant and throw `TemplateOverrideException` out of a type initializer. `HeddleTemplate.Configure` existed but latched on first call, so a second call was silently dropped | ✅ **The engine loads nothing.** The walk, the dependency-context enumeration and the scan-all discovery are deleted; the assembly set is now what the host has already loaded **from disk into the default load context** (observed, never loaded — an in-memory assembly, a collectible or custom context, or an assembly not yet loaded is invisible), plus whatever the host **registers**. `[ExportExtensions]` is read per assembly at registration time, so a loaded-but-unregistered assembly offers no name. **What stops working:** a host that declares `[ExportExtensions]` in an assembly it never registers — its extensions no longer register, and a template calling one fails with `HED0002` (`Cannot find extension`), or, if precompiled, degrades per request with an `ExtensionBindingMismatch` naming the unresolved extension. A **second, narrower population**: a template naming a model or declaration type by string (`@model(Some.Lib.Order)`) in an assembly the host references but has never touched — the walk force-loaded it, so the spelling resolved; now it resolves only once that assembly is loaded or registered, and otherwise raises the same `Couldn't resolve type` the engine has always raised for an unknown spelling. *(Amended 2026-07-27: an adversarial review found the first implementation both narrower and more fragile than this row claimed — it keyed observation on `Assembly.Location`, which is empty for the entire application in a single-file or WASM publish, and it never rebuilt the type maps from observation, so an assembly loaded after the first resolution stayed invisible and the outcome depended on load order. Both are fixed; the row's rule is what ships.)* Every first-party template in the repository and all ten samples resolve unchanged, because using a type is what loads its assembly and a host that renders against a model has used it. **What to call instead:** `HeddleTemplate.Register(assembly)` per providing assembly (the added API; `Configure` is kept as its alias, with the one-shot latch removed so repeated calls now take effect). All ten samples already called `Configure` and needed no change; `Heddle.Generator.IntegrationTests` registers itself from a module initializer — the pattern a host follows. Two consequences worth naming: the `Microsoft.Extensions.DependencyModel` **package reference is gone** (the walk was its only consumer), and the engine no longer force-loads a host's dependency graph at type init. Pinned by `AssemblyRegistrationTests`, which compiles and loads an `[ExportExtensions]`-carrying assembly and asserts it offers neither its extension name nor its type spelling until registered, and holds `beforefieldinit` on `AssemblyHelper` so a reintroduced static constructor reddens. **Disposition home:** the Q8.37 ruling called for this in [breaking-windows.md](common/breaking-windows.md), which was correct while 2.1 was believed window-less; that document is instruction-only and a landed window item belongs here, so the disposition is this row |

**Accepted residue, recorded rather than glossed.** (a) Roslyn has no per-reference suppression for
`CS8002` — the warning carries no source location and the `Csc` task takes only a project-wide disabled
list — so item 5's mechanism is *keyed on* the named third-party assembly rather than scoped to it: a
project referencing both a listed and an unlisted unsigned assembly would silence both. No project does.
(b) `src/Heddle.Performance` is under a change-nothing ruling (Q7.2); the only edit made there is the
deletion of its dead `<Version>` element, which item 4 requires and which needs no other change to that
project.

| **E10 (NORMATIVE narrowing; no earlier resolution reopened).** The code-sharing program's authority convention read *"expression semantics defer first to `native-expressions.md`"* — unconditional authority for prose that nothing checked. Evidence that unconditional authority over unchecked prose is a defect-generating mechanism, not a style question: a claim-by-claim audit of that document found **four outright false normative statements**, and the shape of each is that a reader doing exactly what the convention instructs would break working code. The anchor case, deviation 1, said `==` on "reference/**mixed**" types falls back to a total `object.Equals`; the runtime guards that fallback with `IsReferenceish(left) && IsReferenceish(right)`, so a reference/value mix is a positioned `HED1008` on **both** tiers. Following the convention means deleting the guard and shipping `string == int` silently comparing `false` where it used to be a compile error — and the review would have approved it, because the plan of record said so. The other three are the same shape: deviation 6 overstated user-defined operator support (honoured for arithmetic, relational, equality and `??`; not at all for bitwise, shift and unary), the shift row understated what the engine accepts (any integral right operand, so `I << L` compiles here and is `CS0019` in C#), and the lifted-operands section claimed relational *and equality* lift "exactly as C#" when lifting is numeric-path only. Rehearsed rather than argued: with the guard replaced by `if (true)`, exactly one test reddened — the cross-tier pin already existed, so the trap was in the prose alone. **Amendment:** documentation authority becomes **mapped and conditional** — a claim block has exactly one normative home, and that home outranks the implementations only for a claim carrying a verification marker or covered by a gate. An unmarked, ungated claim is *evidence of intent, not an authority*: a contradiction between it and both implementations agreeing is resolved by investigating and recording the outcome, never by editing code to match the sentence. Explicitly **non-retroactive** — resolutions already taken against the unconditional convention stand, because relitigating them on a rule that did not exist when they were taken would be the opposite of what this fixes. What the narrowing does not do is make stale prose unfixable: the doc-is-right-code-is-wrong outcome is unchanged, and four sentences were corrected in the same pass. | [Phase 8 — docs sweep](../generator_plan/phase-8-docs-sweep.md) (D3/WI4), from the Q8.7 ruling | [cross-cutting-decisions.md — D10](common/cross-cutting-decisions.md#d10--documentation-authority-is-mapped-and-it-is-conditional); the code-sharing program README's *Authority convention* bullet becomes a pointer to it |

| **E11 (additive rule; no earlier decision overturned).** [E8](#cross-spec-amendments-ledger) and [E9](#cross-spec-amendments-ledger) made the test suite say which *tier* it exercised and made a test *input* exist once. Neither covered the third artifact a behaviour change leaves stale: the **prose**. Evidence that this is not tidiness — 16 of 85 shipped diagnostic ids were named in no published page (the block a template author is most likely to hit had 4 of its 17 documented, and the id at the centre of [E10](#cross-spec-amendments-ledger)'s false claim was documented nowhere); a settings table could gain or lose a row with nothing noticing, and the VS Code extension's defaults could drift from the engine's so the editor reports diagnostics the host would not; a document could name a member removed in the same release; and `.gitattributes` pinned a golden-corpus path that had not existed since the harness moved, so a `-text` pin its own comment calls load-bearing applied to nothing. Every existing gate was green throughout, because none of them read prose. **Amendment:** testing-standards.md gains a *Documentation currency* section — a change that alters an observable behaviour names the documents describing it in the same landing and either updates them or records why not; four surfaces derive that list from gates (diagnostic ids, option names and defaults, public members, versions) with links and line citations covered separately; what cannot be gated is **dated**, through a per-document footer naming commit and date and marking the claims a gate does cover, which is the marker [D10](common/cross-cutting-decisions.md#d10--documentation-authority-is-mapped-and-it-is-conditional)'s conditional authority keys on; and the residue that is genuinely not gateable is **enumerated** — rationale accuracy, overstatement, omissions, ordering and emphasis — so the gap is not later mistaken for an oversight. | [Phase 8 — docs sweep](../generator_plan/phase-8-docs-sweep.md) (D11/WI15) | [testing-standards.md](common/testing-standards.md) — additive section; no existing rule altered |

## Diagnostic registry corrections

- **2026-07:** `HED3005` (branch continuation/terminal missing `[ScopeChannel]` drift
  warning) and `HED7016` (its build-time generator twin) shipped in 2.0.0 with the
  `[BranchRole]` work ([CHANGELOG](../../CHANGELOG.md)) but were never recorded in the
  [registry](common/cross-cutting-decisions.md#claimed-diagnostic-ids-registry); both
  verified in source and recorded now.

## Cross-spec amendments ledger

The accumulated, append-only entries of the
[ledger mechanism](common/cross-cutting-decisions.md#cross-spec-amendments-ledger).
Entries recorded after the `v2.0.0` release start here.

| Amendment | Made by | Amends |
| --- | --- | --- |
| **E1 (clarification requested; no Phase 1 text change).** Confirm that the mandated Heddle reference row may appear in the wall-time tables of *either* track (presentation rules 1–2 force it, since rule 2's ratio column must anchor to it) and that rule 5's "numbers from different tracks are never mixed" governs *engine result rows*, not the track-agnostic reference excerpt. | Phase 2 (rust) spec | metrics-protocol presentation rules 1–5 (clarification only — the *Amends* target is deliberately a request for a courtesy wording note; no normative rule is altered, so this cell names a clause to clarify, not a decision to overturn) |
| **E2 (stale example; non-normative).** Parity-contract-v2 N5 rule 3's parenthetical "Askama's fixed `&#x27;`" and the Phase 1 README's external-reference line describe the legacy `djc/askama` escaper; askama-rs 0.16.0 emits the all-decimal family (`&#34; &#38; &#39; &#60; &#62;`). Every 0.16.0 spelling is already in N5's recognized set, so **no normative change** — the examples should name the decimal family (or both, dated). | Phase 2 (rust) spec | parity-contract-v2 N5 rule 3 example + Phase 1 README external-reference line |
| **E3 (stale expectation; non-normative).** Phase 5 WI6's done-when phrase 'encoded-loop retained mean ≈ corpus byteLength' assumes 1-byte/char string storage. Measured evidence (implementation pass, 2026-07-21): CPython stores the BMP-Japanese-bearing output at 2 bytes/char (PEP 393), so the correct anchor is the string's in-memory size — retained mean matches `sys.getsizeof(out)` within 0.1% (1,523,428 B vs corpus byteLength 831,685 B, ≈1.83×). The tracemalloc measurement and D11 procedure are correct; the phrase should anchor to `sys.getsizeof`, not byteLength. | Phase 5 (python) implementation review | phase-5-python README WI6 done-when phrase (expectation text only — no normative rule altered) |
| **E4 (NORMATIVE override).** The mandated JS bench body `() => do_not_optimize(render(...))` does **not** measure output production. `do_not_optimize` is `function do_not_optimize(v) { $._ = v; }` in full (`mitata/src/lib.mjs:5`): it makes the value escape so V8 cannot prove it unread, and never walks it. Both engines are `+=` rope builders, so `render()` returns an unflattened V8 `ConsString` and the timed region only ever allocates rope cells. Measured evidence (the 2026-07-22 run, since **withdrawn as invalid** — the defect below was confirmed independently at source level, and the fix's effect re-confirmed on 2026-07-25): `eta`/`composed-page` = 330.7 ns for 34,847 B of golden output ≈ **105 GB/s**, above one core's store bandwidth on that machine, corroborated by mitata's own heap delta of ~945 B/render (3% of output size). Post-fix the same cell measures 4,273 ns / 8.2 B/ns (2026-07-25). Note the corroborating heap signal is **not** a sound materialisation test in general — see the plausibility register in that report's consolidated tables. **Amendment:** the body becomes `() => do_not_optimize(flatten(render(...)))`, where `flatten` forces materialisation via `%FlattenString`; D11's rejection of "manual sink variables" is narrowed to *sinks that replace* `do_not_optimize`, not a materialisation call composed with it; and D11's `--allow-natives-syntax` justification extends from DCE detection to this primitive. Note `s.length` is **not** a valid alternative — V8 stores the length on the `ConsString`, so reading it is O(1) and never flattens. Also recorded: **D12 cannot detect this class of defect** — `mitata/src/main.mjs:531` flags `!` only when `stats.avg < 1.42 × noop.avg` against an empty-function baseline, so a 330 ns cell never trips it and `DEOPT-CHECK: clean` is fully compatible with the artifact. D12 detects *total* elimination, not partial work elision, so a companion `MATERIALISATION-CHECK` (implied throughput vs a physical ceiling, plus a hard failure if the flatten primitive fell back) is added alongside it rather than replacing it. | Benchmark-defect remediation pass, 2026-07-25 | [phase-4-js D11](cross-stack-benchmarks/phase-4-js/README.md) benchmark-body Decision clause + its alternatives-rejected clause + its flag justification; [phase-4-js D12](cross-stack-benchmarks/phase-4-js/README.md) sufficiency claim; [harness-and-run.md](cross-stack-benchmarks/phase-4-js/harness-and-run.md) §mitata run shape code block and Flags/Renderers bullets |
| **E5 (NORMATIVE override).** Phase 1 recorded that the Razor twin "remains outside every gate, as today" and that "`ParityCheck.Twins()` continues not to yield it". Both were back-compat statements — Phase 1 was not touching Razor — not rulings that a non-parity row belongs inside a protocol suite. Evidence that the arrangement is untenable: `RenderRazor` sat in `TextRenderBenchmarks` (the `composed-page` protocol suite, whose premise is identical work) while rendering the full `Views/layout.cshtml` page, a different and larger payload; it carried a hand-duplicated 56 KB copy of `AreaComponent.Areas` which had **already silently drifted** from the original (`layout.heddle`'s `logo-holder` contains `<a href="/">`, `layout.cshtml`'s is empty); and `RazorCustomStyles` was dead code invoked from no view, so Razor never emitted the `/* CSS Comment Test */` fragment Heddle does emit. Publishing the row therefore required bespoke non-comparability plumbing in the report layer. **Amendment:** Razor is brought **under** the composed-page parity gate as a proper twin, authored like the existing Liquid/Scriban/Handlebars twins against the shared `TwinContent` fixtures (so it cannot drift), and the full-page workload plus its duplicated fixture tree are removed. `ParityCheck` gains a host-aware overload because Razor renders through MVC DI; the `parity` verb builds that host lazily so the one-command proof covers all five twins. Razor's published figure changes completely as a result and had to be re-measured before the "faster than Razor" claim could be restated. Re-measured 2026-07-25: **Heddle 30.52 μs vs Razor 41.66 μs on byte-identical output** with all five twins matching, against the withdrawn run's non-parity 63.19 μs for the old larger workload (2026-07-25). | Benchmark-defect remediation pass, 2026-07-25 | [parity-contract-v2](cross-stack-benchmarks/phase-1-cross-stack-foundation/parity-contract-v2.md) "What v2 changes for existing artifacts" Razor-twin row; [phase-1 README](cross-stack-benchmarks/phase-1-cross-stack-foundation/README.md) §Back-compat and migration Razor-twin bullet |
| **E6 (NORMATIVE override).** Each phase set its own harness run lengths independently, every one of them defensible in isolation, and nobody ever compared the totals. Measured on the 2026-07-22 run (`summary.txt`, measure phase). That run's *render figures* were later found invalid and its report withdrawn, but the figures used here are per-step wall-clock durations — how long each harness took — which the invalidation does not touch: **JS 1.0 s/cell** (32 cells in 31 s) · Rust 15 · Python 17 · Go 26 · .NET 32 · **JVM 503 s/cell** (32 cells in 16,096 s). The JVM leg alone was **83% of the whole 5.45 h session** and 16–34× the per-cell cost of the middle four, while the JS leg was 15–32× *below* it — a 500× spread in measurement effort across ecosystems whose results are printed in the same ranked tables. Neither extreme was chosen; both are what their harness happened to default to. **Amendment: one uniform measurement budget of ~10 min per ecosystem**, allocated per harness to whatever term its own noise actually lives in. Per ecosystem: **JVM** — JMH's stock defaults (`@Fork(5)`, `5 × 10 s` warmup, `5 × 10 s` measurement) become `@Fork(3)`, `@Warmup(1 × 2 s)`, `@Measurement(3 × 1 s)`. Sized from the measured variance decomposition (15 cells with complete forks, tuned Linux run at `governor=performance, boost=0`): within-fork RSD across five 10 s iterations **0.18% median / 1.45% max**, so per-iteration length was pure over-provisioning; warmup iteration *1*'s own 10 s average already within ~1% of iteration 5 (iterations 2–4 deviate 0.36% median / 2.34% max), so the JIT transient is short relative to one iteration; fork-to-fork RSD **1.18% median / 3.28% max**, i.e. the dominant term is the one forks exist to sample, which is why forks stay plural. 5 → 3 forks moves the 2σ limit on a pairwise gap from 1.49% to 1.93%; the tightest comparisons the report actually makes are 4.04% and 5.46%, and the only sub-2% pairs in the data (jte `composed-page` 0.82%, thymeleaf `composed-page` 0.27%) were **already unresolvable at 5 forks**, so nothing that was ever resolvable is lost. **.NET** — BenchmarkDotNet's adaptive defaults become `[ShortRunJob]` (`LaunchCount 1, WarmupCount 3, IterationCount 3`) on the eight protocol suites; measured effect `SubstitutionRenderBenchmarks` 119.1 s → 48 s. `[MemoryDiagnoser]` stays on, so the allocation column is unaffected. **Go** — `-count=20` → `-count=14`, still above benchstat's ≥10 working guidance though below its "ideally 20". **Python** — the four render scripts keep pyperf's default 20×3×1 (32 cells in 546 s is already inside the budget); only the non-comparable cold-compile sidebar (D10) drops from 20 processes to 7, having cost 288 s. **Rust** — unchanged at 8.3 min, already in band. **JS** — the interesting case: mitata exposes **no per-cell time budget through its public API** (`B.run()` constructs its own options object internally and `run()` forwards only `throw`, so the 642 ms `min_cpu_time` is unreachable without forking the library). JS therefore spends its budget on **independent processes** rather than one long run: 18 repeat passes per render track via `run.sh --repeat`, collapsed by the new `bench/aggregate.mjs` into the published artifact, where `stats.avg` is the **median of the per-pass averages** and `stats.min`/`max` span the passes, so the published dispersion is cross-process variation. This is the same term JMH buys with forks and pyperf with its 20 workers, and it is exactly what a single-process harness lacks — so for JS, spending the budget this way buys the missing dimension rather than more digits on one it already had. It also **discharges D13**: the five-run stability procedure stops being a separate publication-gating step that someone has to remember (the 2026-07-22 run shipped JS numbers with no RSD verdict at all, disclosed as a limitation) and becomes the measurement itself, with `stability-summary-<track>.md` and a `STABILITY:` verdict emitted every run and a `failed` verdict exiting non-zero. Executed evidence: 18 passes of `bench/controlled.mjs` in 279 s → `STABILITY: verified`, cross-pass RSD median 1.81% / max 2.85%. D11's "never two runs" invariant is preserved per pass (each pass still writes its text and JSON views of one run, under `stability/<track>/run-<k>.*`) and the aggregator overwrites the published `<track>.txt` so the two published views cannot disagree. Expected session total **≈ 1 h**, down from 5.45 h. **Not confirmed on the protocol machine.** Every figure above is from Linux; the two candidate JMH regimes trialled on this workstation disagreed by 6% on a 1.5 µs cell under `governor=powersave, boost=1` with an interactive desktop live, which is ambient noise and not regime effect. A full `--budget baseline` run has since executed all six ecosystems end to end on Linux (2026-07-25): 16.8–28.5 min per ecosystem, every gate and every measurement step exit 0, dispersion no wider than before (.NET's `composed-page` SD *fell* to 3.5% of the mean). Confirmation on the **Windows protocol machine is TBD** — that run is pending a re-test after the 2026-07-22 results were invalidated — and the point of this amendment is that the re-test now costs about an hour instead of a working day. | Benchmark run-length parity pass, 2026-07-25 | [phase-3-jvm D9](cross-stack-benchmarks/phase-3-jvm/README.md#d9--jmh-configuration-pin-137-explicit-run-length-regime-return-value-blackhole-gc-profiler-dce-plausibility-rule) run-length Decision clause, its Rationale wall-clock estimate, and its "fewer forks/iterations" alternatives-rejected clause; [harness-and-jmh](cross-stack-benchmarks/phase-3-jvm/harness-and-jmh.md#jmh-benchmark-shape-d9) class-shape block + measurement-procedure duration; [metrics-protocol](cross-stack-benchmarks/phase-1-cross-stack-foundation/metrics-protocol.md) .NET harness row (BenchmarkDotNet defaults); [phase-4-js D11](cross-stack-benchmarks/phase-4-js/README.md) "no custom sampling parameters" + single-`run()` publication clause and [D13](cross-stack-benchmarks/phase-4-js/README.md) five-run procedure status; [phase-5-python D8](cross-stack-benchmarks/phase-5-python/README.md) pyperf-defaults clause (cold-compile script only); [phase-6-go D9](cross-stack-benchmarks/phase-6-go/README.md) `-count=20`; [phase-8 harness-settings](cross-stack-benchmarks/phase-8-linux-crosscheck/harness-settings-and-validation.md) regime-identity bullet + [phase-8 README](cross-stack-benchmarks/phase-8-linux-crosscheck/README.md) mirrored-decisions row and session budget |
| **E7 (NORMATIVE override).** Report-assembly pins "Workload order everywhere: the protocol order 1–8". That order was set before any run existed and it puts `composed-page` first, which turns out to be the single worst workload to lead with. Two independent defects were measured on the 2026-07-25 run. **(a) A sizing regime the protocol never modelled.** The CLR allocates any object of **85,000 bytes or more on the Large Object Heap**, and .NET strings are UTF-16, so a rendered page crosses that line at 42,500 characters and every render past it drives a full, blocking Gen2 collection. Measured on the run's box, `new string(ReadOnlySpan<char>)` sustains **40.3 B/ns at 84,800 B and 8.9 B/ns at 108,680 B** — a 4.5× step at the threshold — and 20,000 Heddle `composed-page` renders produce **76 Gen0 and 75 Gen2 collections**. Three of the eight workloads (`composed-page` 108,802 B, `large-loop` 385,560 B, `encoded-loop` 1,663,370 B as UTF-16) are above it; five are comfortably below. No other ecosystem has this cliff — their outputs are UTF-8 or Latin-1 and stay on the normal heap — so the .NET column on those three rows pays a cost structural to the runtime and absent everywhere else. **(b) `composed-page` contains no per-item computation.** Its output is 17 pre-existing string fragments concatenated: no loop body, no branch, no escaping. Every compiled engine reduces it to ~17 `memcpy` calls, so the row measures memory bandwidth and the allocator, not template execution. Floors measured on the same box: a raw 55,466-byte `memcpy` **438 ns**; `String::with_capacity` + 17× `push_str` **476 ns**; Askama **659 ns**; .NET `string.Concat` of the same 17 fragments **12,537 ns**; `StringBuilder` + `ToString()` **26,320 ns**; Heddle `Generate()` **26,432 ns**. Heddle is within ~2× of the .NET floor and marginally faster than a plain `StringBuilder` — leading the report with a row where Heddle ranks #11 of 16 therefore mis-states the finding, since there is almost no engine work left to win on it. **Amendment:** workload order in the *report* becomes **tier 1 (realistic sizing) before tier 2 (edge-case sizing), ascending by rendered size within each tier**, with the tier **derived** — never declared per workload — from whether rendered output reaches the LOH threshold as UTF-16. The protocol numbering 1–8 is unchanged and remains what `manifest.json` and the phase specs record; this governs presentation only. Ordering moves **into `consolidate.py`**, which now emits a `## Sizing regimes` section stating the derivation and a second generated artifact, `summary-tables.md`, that `index.md` embeds via VitePress `<!--@include:-->` so the narrative page transcribes no measured figure by hand; `--check` covers both files. The master runners invoke the script as a recorded non-fatal step, so a published directory is a copy of an out dir rather than a hand-assembled one. **Also corrected: the implied-throughput numerator.** The contract specifies "golden byteLength / ns", but the golden oracle is stored in its **normalized** form (`TwinContent.Normalize` collapses every inter-tag whitespace run before export), so on `composed-page` it is 1.56× smaller than what engines emit — 34,847 B stored against 54,401 chars rendered (JS 54,340 B, Rust 55,466 B). Seven of the eight workloads agree within 2.4%; `composed-page` was the sole divergence, and dividing by the golden there understated every engine by that factor **and hid a ceiling violation**: Askama's cell is 51.2 B/ns, not the 32.8 published. The numerator becomes the rendered size, carried in `RENDERED_CHARS` with a documented re-measure procedure and a footnoted fallback to the golden for any unmeasured workload. Recorded alongside: the 50 B/ns plausibility ceiling is **conservative for modern hardware** — a raw 55,466-byte `memcpy` is 126 B/ns on this box — so a flag means "look at this cell", not "this is impossible"; the authoritative materialisation control remains the harness-side `MATERIALISATION-CHECK` (E4). | Report presentation review, 2026-07-25 | [report-assembly](cross-stack-benchmarks/phase-7-consolidated-report/report-assembly.md) §`index.md` normative structure workload-order clause + §`consolidate.py` contract generate-mode emit clause and implied-throughput numerator |
| **E8 (additive rule; no earlier decision overturned).** [testing-standards.md](common/testing-standards.md) pins suite homes, fixture conventions and regression gates, but says nothing about which *execution tier* an end-to-end test actually exercised. For the source generator that gap is load-bearing: the precompiled tier and the dynamic engine are byte-identical by design, so a precompiled→dynamic fallback changes no output and no assertion. Evidence that the gap is not theoretical — the research program verified two shipped drifts of exactly this shape (content-hash input mismatch, nested/generic AQN mismatch), and confirmed at source that **no test rendered real generator output through the resolver/gauntlet path with fallback treated as failure**: the differential harness invokes generated entry points directly (bypassing `PrecompiledTemplates.Register`, `TemplateResolver.ConsultPrecompiled` and every `PrecompiledGauntlet` check), the ~27 gauntlet tests run against hand-built manifests, and exactly one end-to-end test pinned a resolver-served adapter — incidentally, because its resolver root did not exist. Re-confirmed executably by the phase 0 seeded-mismatch meta-suite's negative control: a corrupted manifest row renders byte-identical output under an unguarded render. **Amendment:** testing-standards.md gains a *Precompiled-tier posture* section — end-to-end precompiled tests pin the tier (Strict + fallback sentinel, any fallback fails the test); fallback is tested only by tests that declare the expectation explicitly (`ExpectDegrade` / `FallbackGuard.Expect`, which must remain the exhaustive greppable list); new feature areas contribute templates to the gauntlet-crossing corpus rather than re-plumbing their suites; and a guarded fixture that fails on a known owned defect is quarantined with an owner-naming `Skip`, never weakened. Phases 1–6 of the code-sharing program inherit the rule instead of restating it. | [Phase 0 — test-fallback-guardrails](../generator_plan/phase-0-test-fallback-guardrails.md) (WI8/D8) | [testing-standards.md](common/testing-standards.md) — additive section; no existing rule altered |
| **E9 (additive rule; no earlier decision overturned).** [E8](#cross-spec-amendments-ledger) added the *Precompiled-tier posture*, whose third bullet asks new feature areas to contribute their templates to the gauntlet-crossing corpus. That bullet was recorded as prose with **no mechanism behind it**, and it has never been backfilled. Evidence, re-verified against the tree at `45154fd`: the feature suites build their templates as **inline strings** — so phase 0 D4's coverage rationale that *"the corpus is the union of the feature templates"* is false (corrected in place, 2026-07-26) and roughly **130 feature tests never cross the gauntlet**; the shared corpus is **62** `.heddle` files of which **40** precompile, while `src/Heddle.Tests` alone carries ≈ **780** inline template literals (≈ 1,115 across the three suites); **18 distinct template literals appear character-for-character in both `src/Heddle.Tests` and the generator suites** (corrected in place, 2026-07-26: **shared *substrings*** — re-measured at execution time it is 31 literals over 14 characters, ~21 template-ish, and **none is a whole template on either side**; the templates the two tiers compile had already diverged, which is why Q8.42 declined the unification rather than performing it) (the region / props / `@out` families — e.g. `@panel(){{@%<head:head>{{[h:@foot()]}}<foot:foot>{{[f-filled]}}%@}}` in both `RegionTests.cs` files, and `[@out(){{BODY}}]` in `OutStaticBodyInertTests` / `OutStaticBodyFallbackTests`), agreeing today with nothing gating that they keep agreeing. Contributing a template currently means hand-adding a row to one project's csproj (`Heddle.Tests.csproj` hand-lists **112** corpus files) and reaching it from another project by rewriting an assembly path and climbing `../../..` — a helper triplicated across three files, which until `45154fd` made five tests silently `return`. The generalisation the program itself licenses: **hand-kept duplicate test *inputs* are hand-kept duplicate *rules* one level up** — same mechanism (nothing forces the copies to agree), worse consequence (a differential test's premise that both tiers got identical input stops holding, and no assertion notices). **Amendment:** testing-standards.md gains a *Test-input single-sourcing* section — a shape more than one tier verifies exists exactly once as a shared corpus file; every entry **declares** its intent on two axes (build-tier classification; how it may be exercised) with a mandatory justification and bidirectional completeness gates, because negative probes and deliberate-degrade shapes are ordinary members that a filename convention cannot classify; membership is gated by **set equality against the declaration**, never a count or a floor (a count is satisfiable by editing one digit; this repo shipped a `>= 25` floor against an actual 40, and still carries a `>= 40` floor whose message says "~45 files" against an actual 62); one-line and diagnostic-position probes stay inline, since proximity to the assertion is real value and they gain no cross-tier coverage; shared assets are read from the consumer's own output directory, never by traversal into a sibling project's `bin`; and moves are byte-identical with encoding pinned by a gate — line endings via `.gitattributes`, BOMs by their own assertion, since the two are independent and the program had to normalise inconsistent BOMs across eight generator snapshots (two had one, six did not). E8's third bullet is sharpened, not overturned: it gains the mechanism it was missing. | [Phase 7 — shared test corpus](../generator_plan/phase-7-shared-test-corpus.md) (D8/WI7) | [testing-standards.md](common/testing-standards.md) — additive section; [E8](#cross-spec-amendments-ledger)'s corpus-contribution bullet gains an enforcement mechanism (no existing rule altered) |
| **E12 (NORMATIVE override; supersedes every citation of the retired project).** The .NET leg of this program was `src/Heddle.Performance` — the repository's original intra-.NET benchmark project, grown into the cross-stack anchor. Every phase spec cites it: for the corpus location, for the `parity` and `export-corpus`/`verify-corpus` verbs, for the twin fixtures phases 2–6 transcribed their models from, and for the `--filter *<Suite>*` invocation the metrics protocol pins. **Maintainer ruling, 2026-08-01: that project is decommissioned for comparative benchmarking, and the .NET leg is rebuilt from scratch under `benchmarks/dotnet/`, structured like the other five ecosystem harnesses.** The project is deleted; nothing in the tree may cite its code. Why a rebuild rather than a move: the old project was a *signed* `src/` assembly and a friend of the engine (`InternalsVisibleTo`), so it could and did reach engine internals a caller cannot — and a benchmark that needs private access to the thing it measures is measuring something no user can reach. It also multi-targeted three frameworks for an intra-.NET historical question this program does not ask, stored its templates as escaped C# literals rather than as reviewable files, measured one fairness track, and covered Razor on one workload of eight. **What the replacement is, concretely.** A single unsigned `net10.0` project with a verb-dispatching entry point mirroring every other harness: `gate` (all 143 registered cells — byte gate on the controlled track, functional verifier on the idiomatic one, security floor on the encoded workloads, plus a materialisation trailer and a stated precompiled-coverage figure), `selftest` (141 checks including the six-technique differential), `export-corpus` / `verify-corpus` (unchanged contract), and four bench verbs — `bench-crossstack` (eight suites, one per workload, six engines, **both fairness tracks**), `bench-techniques`, `bench-cold`, `bench-internal`. The engine is reached through its **public** surface only; the `InternalsVisibleTo` grant is withdrawn. **Evidence the replacement is faithful:** exporting the corpus with the new tool reproduces all sixteen committed artifacts — eight goldens and eight `.verify.json` sidecars — **byte for byte**, and `verify-corpus` passes freshness and all twenty calibration cases. **What this amends, in every phase spec at once:** read `benchmarks/dotnet/GoldenCorpus/` as `benchmarks/dotnet/GoldenCorpus/` (the files moved under [E8](#cross-spec-amendments-ledger); the bytes never changed); read the `parity` verb as `gate`; read `dotnet run -c Release --project src/Heddle.Performance -f net10.0 -- <verb>` as `dotnet run -c Release --project benchmarks/dotnet -- <verb>`; read every `Runners/*.cs` or `TestSuite/**` citation as the corresponding file under `benchmarks/dotnet/src/`, and `Runners/README.md` as `benchmarks/dotnet/GoldenCorpus/README.md`. Two consequences are **not** notational. (1) The metrics protocol's .NET row named suites `<Text|Substitution|…>RenderBenchmarks`; the suites are now named after the workloads they measure (`ComposedPageBenchmarks`, `TrivialSubstitutionBenchmarks`, …), matching the JVM harness's convention, and `benchmarks/report/consolidate.py` maps artifacts by those names. (2) **Phase 1 D15's controlled-track-only scope for .NET is discharged**: the leg measures both tracks, which retires the standing limitation that .NET had no idiomatic row to anchor the idiomatic tables to. Budget consequence, stated rather than discovered later: the .NET measure phase roughly doubles, and the ~9 min / ~21 min figures in [E6](#cross-spec-amendments-ledger) are now low. Also retired with the project: `Directory.Build.targets`' CS8002 acceptance mechanism, which existed solely because one *signed* project under `src/` referenced unsigned Scriban — no project under `src/` references a competitor engine any more. The vendored third-party Fluid harness that lived inside the project is **preserved, not deleted**: it is an independent deliverable with its own upstream obligation, and it moves intact to `benchmarks/third-party/`. | Maintainer ruling, 2026-08-01 | Every `src/Heddle.Performance` citation in [phase 1](cross-stack-benchmarks/phase-1-cross-stack-foundation/README.md) (README, golden-corpus.md, metrics-protocol.md, parity-contract-v2.md, workloads.md), [phase 2](cross-stack-benchmarks/phase-2-rust/README.md), [phase 3](cross-stack-benchmarks/phase-3-jvm/README.md), [phase 4](cross-stack-benchmarks/phase-4-js/README.md), [phase 5](cross-stack-benchmarks/phase-5-python/README.md), [phase 6](cross-stack-benchmarks/phase-6-go/README.md) and [phase 8](cross-stack-benchmarks/phase-8-linux-crosscheck/README.md); [metrics-protocol](cross-stack-benchmarks/phase-1-cross-stack-foundation/metrics-protocol.md) .NET harness and suite-name rows; [phase-1 D15](cross-stack-benchmarks/phase-1-cross-stack-foundation/README.md) controlled-track-only scope (discharged); [coding-standards](common/coding-standards.md) and [testing-standards](common/testing-standards.md) benchmark-project rows; the 2.1 as-shipped record's change-nothing note on that project (moot — the project is gone) |
| **E13 (NORMATIVE override; changes the unit E6's budget is measured in).** [E6](#cross-spec-amendments-ledger) set *one uniform measurement budget of ~10 min per ecosystem*, and every leg it was sized against carries **two** engines — Rust (Askama, Tera), JVM (JTE, Thymeleaf), JS (handlebars, eta), Python (Jinja2, Mako) — or three (Go). For those legs the leg total and the per-engine share are nearly the same number, so the distinction never surfaced. The .NET leg carries **six**: Heddle, Fluid, Scriban, DotLiquid, Handlebars.Net and Razor. Holding it to the same *leg* figure divides one ecosystem's budget across three times as many engines, so every .NET cell is sampled about a third as well as a Rust cell — and the cross-engine comparison this program exists to make would rest on its worst-sampled rows. **Amendment: the budget unit is ONE ENGINE, not one ecosystem.** `short` and `baseline` keep their ~10 min and ~30 min sizes; what they size is an engine's share. A leg's total is its engine count times that budget, so the .NET leg is longer than the others by construction rather than by accident. **How .NET spends it — launches, and only launches.** Two reasons, one practical and one substantive. Practical: the leg is overhead-bound (one process per benchmark case, plus JIT and `[MemoryDiagnoser]`), so measured on one 12-cell suite of this repo the wall clock is very nearly linear in `LaunchCount` — **139 s at `LaunchCount 1`, 316 s at 2**, about 14.8 s per cell per additional launch — which makes the knob predictable, whereas raising the iteration counts instead runs through BenchmarkDotNet's pilot stage and does not (a trial at `--launchCount 4 --warmupCount 5 --iterationCount 8` was abandoned after overshooting its projection by more than 2x). Substantive, and the real reason: `LaunchCount 1` samples the within-process term ONLY — one process, one JIT, one heap layout — and never samples the cross-process term at all. E6 itself established that this is the term worth paying for: it kept JMH's forks plural after measuring fork-to-fork RSD at 1.18% median against a within-fork 0.18%, and it re-spent the entire JS budget on independent processes for exactly this reason. The .NET leg was the one that never bought it. **The shapes, measured not projected:** `short` is BenchmarkDotNet's `ShortRun` with **`LaunchCount 2`** (26.3 s/cell); `baseline` is **`--launchCount 6`** over the same warmup and iteration counts (~69.5 s/cell, one launch beyond the last measured point at `LaunchCount 5` = 58.7 s/cell, and the only projected figure here). Over the leg's 151 cells (96 cross-stack — 8 workloads x 6 engines x 2 tracks — plus the techniques, cold and internal sidebars) that is **66 min short / 175 min baseline**, i.e. **11 / 29 min per engine**. The default job also moves out of a `[ShortRunJob]` attribute into the harness's own configuration: an attribute job cannot be *replaced* from the command line, only added to, so `--job Dry` had been running the full measurement alongside the dry one and a smoke pass cost more than a measurement pass. **Recorded and deliberately not acted on:** the other five legs are still sized per *ecosystem*, which leaves them at roughly 3-5 min per engine against .NET's 11. Making the program uniform per engine means roughly doubling every one of them, and that is a session-length decision for the maintainer, not a consequence of this amendment. | Benchmark budget review, 2026-08-02 | [E6](#cross-spec-amendments-ledger)'s per-ecosystem unit and its .NET row (`[ShortRunJob]` + `--warmupCount 7 --iterationCount 15`); [metrics-protocol](cross-stack-benchmarks/phase-1-cross-stack-foundation/metrics-protocol.md) .NET harness row; benchmarks/README.md measurement-budget tables |
| **E14 (NORMATIVE override; completes E13 and corrects its .NET arithmetic).** [E13](#cross-spec-amendments-ledger) changed the budget's unit from one ecosystem to one engine, resized the .NET leg accordingly, and recorded — without acting on it — that the other five legs were still sized per ecosystem and therefore sat at roughly 3-5 min per engine. **Maintainer ruling, 2026-08-02: resize every leg so each engine gets ~10 min at `short` and ~30 min at `baseline`.** **An engine is 16 cells** — eight workloads x two fairness tracks — and that definition is what makes the budget checkable rather than a matter of taste. It also corrects E13, which computed .NET's per-engine share by dividing the whole 151-cell leg by six engines and got 11 min. That over-credits: 55 of those cells are the Heddle-only techniques, cold and internal sidebars, which belong to no engine's comparison share. The comparable rows are the 96 cross-stack cells, so the real figure at E13's `LaunchCount 2` was **7.0 min per engine**, not 11 — under budget, not over. `LaunchCount 3` puts it at ~9.9 min and `LaunchCount 10` at ~30.1 min; the sidebars ride at the same job shape on top. **The knobs, all six.** .NET `ShortRun + LaunchCount 3` / `--launchCount 10`; Rust criterion warm-up 3 s -> 5 s and measurement 10 s -> 26 s / `--warm-up-time 10 --measurement-time 84`; JVM `@Fork(3)` -> `@Fork(5)` and `@Measurement(3x1s)` -> `@Measurement(5x1s)` / `-f 5 -wi 2 -i 17`; JS 18 -> 38 aggregated passes / 114; Python pyperf `--values 6 --warmups 1` / `--values 20 --warmups 2`; Go `-count 14` -> `28` / `--count 84`. Each ecosystem's committed source or script default remains its `short` shape, so a bare single-harness invocation is still the budgeted regime. Where a leg had a choice about where to spend the increment it went to the term its own variance decomposition says is dominant — JVM buys forks rather than iterations, .NET buys launches for the same reason (E13), JS buys independent processes (E6). **Cost, stated rather than discovered.** Session totals become **~3.2 h short and ~9.7 h baseline**, against ~1 h and ~2.5 h under E6's per-ecosystem unit and 5.45 h before E6. That is what sampling seventeen engines' worth of comparison rows equally costs, instead of sampling six of them a third as well as the rest. **Every duration in the resized table is PROJECTED** — E6's measured per-leg durations scaled by the knob change — with the single exception of .NET's per-cell cost, measured here at 139 s / 316 s / 704 s for one 12-cell suite at `LaunchCount` 1, 2 and 5. The first protocol run replaces the whole column with real durations, and the projections should be checked against it before any of these numbers is quoted. | Maintainer ruling, 2026-08-02 | [E13](#cross-spec-amendments-ledger)'s .NET per-engine figure (corrected) and its 'recorded and deliberately not acted on' clause (discharged); [E6](#cross-spec-amendments-ledger)'s per-ecosystem run lengths for all six legs; [phase-3-jvm D9](cross-stack-benchmarks/phase-3-jvm/README.md) run-length regime; [phase-2-rust D9](cross-stack-benchmarks/phase-2-rust/README.md) criterion settings; [phase-5-python D8](cross-stack-benchmarks/phase-5-python/README.md) pyperf defaults; [phase-6-go D9](cross-stack-benchmarks/phase-6-go/README.md) `-count`; [phase-4-js D13](cross-stack-benchmarks/phase-4-js/README.md) pass count; benchmarks/README.md measurement-budget tables |
| **E15 (NORMATIVE; records a deletion and its consequence for every citation of it).** **Maintainer ruling, 2026-08-02: `docs/benchmarks` keeps the latest run only.** The three published reports — `2026-07-11`, `2026-07-18` (pre-protocol, intra-.NET) and `2026-07-25` (the first cross-stack run, self-declared as carrying no protocol standing) — are removed, together with an untracked raw-artifact dump. The ruling's premise is that all three were measured by harnesses that no longer exist: the first two by the decommissioned `Heddle.Performance` project ([E12](#cross-spec-amendments-ledger)), the third by the pre-flip .NET leg whose Heddle row was the utf8 sink and whose sink rows were timed through the gate's checksum writers. **Consequence, and why it is recorded rather than quietly absorbed.** 46 markdown links across 16 documents pointed into those folders, and roughly half of them are not decorative — they are evidence citations in ratified plan and spec documents, attached to specific findings (Handlebars.Net at 0.46x allocation on trivial-substitution, the ~8% large-loop gap, the 2.0x composed-page lead, the honest-reporting posture those reports model). Repointing them at the surviving run would have been the cheap fix and a false one: `2026-08-02` does not contain those findings and never measured them. They are therefore **de-linked, with their visible text preserved** — the prose still names the run it is citing, and a reader can see that the artifact behind it is no longer published, which is the truthful state. `DocumentationLinkTests.EveryDocumentedFileLinkResolves` is the gate that forced the choice to be made explicitly rather than left to rot. The VitePress sidebar loses the same three entries; `2026-08-02` gains its own once its `index.md` is written. **What is NOT claimed:** that the removed numbers were wrong. They were honest measurements of their own harnesses; they are removed because a repository that publishes one run should publish the current one, and because keeping reports whose harness has been rebuilt twice invites exactly the cross-harness comparison this program forbids. | Maintainer ruling, 2026-08-02 | `docs/benchmarks/2026-07-11`, `2026-07-18`, `2026-07-25` (deleted); 46 citations across `README.md`, `docs/architecture.md`, `docs/building.md`, `benchmarks/README.md`, `docs/plan/*`, `docs/spec/records.md`, `docs/spec/cross-stack-benchmarks/*` (de-linked); `docs/.vitepress/config.mts` sidebar |
| **E16 (NORMATIVE override; a breaking change and a new registry rule).** Two rulings, one change. **(1) Constant division by zero fails the compile on both tiers.** An integral or `decimal` `/` or `%` over constant operands whose divisor folds to zero — `@(1/0)`, `@(1%(1&0))`, `@(1.0m/0m)` — used to be treated as tier asymmetry to preserve: C# refuses it as `CS0020`, the engine's expression tree compiled it and threw `DivideByZeroException` at render, and `ConstantFolding` therefore **degraded** the template so the "tier whose behaviour is the contract" could keep rendering. The premise was wrong: that expression has no behaviour to preserve — rendering it can only ever throw — so the deferral protected a guaranteed failure, and 22 differential rows pinned the silence as correct. Both tiers now refuse at compile time under **`HED1018`**: the engine raises it from `NativeExpressionCompiler` (positioned at the operator), and the generator, whose `ConstantFolding` proves the same fact, reports a build **error** at the same site. Scope is exactly C#'s: the whole division constant (a runtime divisor that happens to be zero still throws at render, as C# compiles `x/0` without complaint — verified against Roslyn, 0 errors), integral and `decimal` only (floating point folds to `Infinity`/`NaN` and stays legal), the divisor *folded* rather than spelled (`1/(1-1)` counts). **Breaking**, deliberately: a template carrying `@(1/0)` in a branch that never rendered used to compile, and no output-producing use existed to protect. **(2) Same fact, same id.** The generator forwards `HED1018` through the forwarded-descriptor channel instead of claiming a `HED7xxx` twin, and the claimed-ID registry now states the rule: a twin id is claimed only where no engine-compile-time counterpart exists to forward. Existing twins predate the rule and keep their ids (an id once listed is never renumbered); every new diagnostic follows it. | Expression-tier parity pass, 2026-08-02 | [native-expressions.md](../native-expressions.md) diagnostics table (`HED1018` row added); [cross-cutting-decisions.md](common/cross-cutting-decisions.md) claimed-ID registry (`HED1001`–`HED1018` + the same-fact-same-id rule); `ConstantArithmeticDifferentialTests.ConstantDivisionByZeroDegradesInsteadOfBreakingTheBuild` (inverted — the name it pinned is the behaviour this entry retires) |
| **E17 (NORMATIVE; applies the same-fact-same-id rule across the native-expression block).** [E16](#cross-spec-amendments-ledger) established the rule and forwarded one id (`HED1018`); every other native-expression refusal the generator could *prove* the engine repeats still degraded silently — reported only through the aggregate `HED7031` notice — so the consumer discovered the engine's `HED1xxx` error at the runtime template compile instead of at build. The generator now forwards the engine's id as a build **error**, composing the engine's exact sentence from the operand estimator's types (a mirror of the engine's `FriendlyName` spelling): `HED1003` (method-call syntax in a native expression), `HED1004` (a native expression under a *declared*-`dynamic` model, and a path crossing a `[Dynamic]` property), `HED1005` (`&&`/`\|\|` over a non-`bool` operand), `HED1007` (un-unifiable ternary arms and coalesce pairs), `HED1008` (binary `NotDefined` verdicts, the `ulong`-meets-signed pairing included), `HED1009` (unary `NotDefined` verdicts), `HED1010` (a known target type with no accessible indexer), `HED1011` (a non-`bool` ternary condition). The soundness claim is the lockstep sweep's: a `NotDefined` verdict means the engine errors on every input, per TFM. What the writer cannot prove keeps degrading silently, deliberately: an `Unknown` operand estimate; a runtime-owned verdict (an operator witness neither `Bound` nor `Absent` — `Money + 1` stays a degrade because *some* `+` operator exists on `Money`); a type spelling in the engine's sentence the generator cannot reproduce exactly (a generic's `Type.Name` from a symbol-less composite operand); an **undeclared** model type — the engine types such a template from whatever `CompileContext` the host supplies (the corpus's `expr-flagship.heddle` renders exactly that way), so only the in-file `@model(){{dynamic}}`, which overrides the caller's model type on every engine compile, is a proven fact; and a coalesce over a non-null-assignable left, which is the engine's `HED1006` — outside the forwarded set. The template still degrades (no entry is emitted), which no longer matters: the build fails the way the engine's template compile would. `HED7025` predates the rule and stays as-is. | Expression-tier parity pass, 2026-08-02 | [cross-cutting-decisions.md](common/cross-cutting-decisions.md) claimed-ID registry `HED1001`–`HED1018` row (forwarded set recorded); [native-expressions.md](../native-expressions.md) diagnostics preamble; [precompilation.md](../precompilation.md) build-time diagnostics; `OperatorGuardDifferentialTests` / `IndexerDifferentialTests` / `NativeThisOperandTests` (degrade-only expectations inverted to forwarded errors) |
| **E18 (NORMATIVE override).** **Maintainer ruling, 2026-08-08: the Node.js pin is the major line `v24.x`, not the exact point release.** Phase 4 D2 pinned the exact string `24.18.0` with `engine-strict=true`, so even a same-major point release was refused by `npm ci` and the runner's fallback was to disable engine enforcement entirely (`--engine-strict=false`) and record a delta. In practice the exactness bought nothing and cost twice: the property the pin exists to hold constant — the V8 generation the JS rows describe — is major-versioned, while every routine LTS point release either blocked the run or ran with engine enforcement OFF, which is how the 2026-08-03 run executed on v26.4.0 with only a warning. Under the major pin, `engines` is `24.x` and `engine-strict` stays on, so the boundary that matters is enforced instead of bypassed: a non-24 Node is refused, a 24.x point release runs. Exactness is not lost, only relocated — `toolchain.json` and every report's environment block record the precise version a run actually used, and cross-run comparisons still quote that recorded version, never the pin. The Linux cross-check's install artifact remains a concrete 24.x tarball (an instance of the pin, not the pin itself). Phase 4 D2's exact-string clause and its WI acceptance phrase ("even a point-release difference like 24.19.0 is refused"), and phase 8's tarball row, are overridden accordingly; the ratified prose stays as written per this ledger's standing practice. | Maintainer ruling, 2026-08-08 | [phase-4-js README](cross-stack-benchmarks/phase-4-js/README.md) D2 pin + WI acceptance clause; [phase-8 environment-and-toolchains](cross-stack-benchmarks/phase-8-linux-crosscheck/environment-and-toolchains.md) Node row; `benchmarks/js/package.json` `engines`; `benchmarks/run-all.ps1` / `run-all.sh` pin check; benchmarks/README.md pins table |
| **E19 (NORMATIVE override).** **Maintainer ruling, 2026-08-08: the CPython pin is the minor line `3.14.x`, not the exact micro release.** The same reasoning and the same mechanics as [E18](#cross-spec-amendments-ledger)'s Node ruling, applied to Python: the property the pin holds constant — the interpreter generation whose performance work the Python rows describe — is minor-versioned, micro releases are bugfix, and an exact-micro pin makes every routine 3.14 update a recorded delta while adding no measurement identity. The pin becomes `3.14.x` in the runners' check and `toolchain.json`; a non-3.14 interpreter still warns and records a delta (SR-3 posture unchanged), and the exact micro a run actually used remains recorded in `toolchain.json` and every report's environment block, which is what cross-run comparisons quote. Phase 8's deadsnakes/source-build fallback stays a concrete 3.14 instance of the pin. Phase 5's `CPython 3.14.6` clauses and phase 8's delta-vs-3.14.6 rows are overridden accordingly; ratified prose stays as written per this ledger's standing practice. | Maintainer ruling, 2026-08-08 | [phase-5-python README](cross-stack-benchmarks/phase-5-python/README.md) D-pins + environment block clause; [phase-8 environment-and-toolchains](cross-stack-benchmarks/phase-8-linux-crosscheck/environment-and-toolchains.md) Python row; `benchmarks/run-all.ps1` / `run-all.sh` pin check; benchmarks/README.md pins table |
| **E20 (NORMATIVE override; supersedes D5/Q1.4, the anchor-freeze for composed-page, and the workload-6 single-tile shape).** **Maintainer-approved redesign, 2026-08-08: `composed-page` becomes a genuine full-page layout workload and `fragment-heavy` becomes four dispatched fragment kinds with one level of nesting.** Both workloads under-delivered on the dimension they claim to own: composed-page's 149-line layout chrome was dead text (the `@<<` child-as-entry-point path dropped it, documented as the fidelity note D5 froze), so ~99% of its golden was memcpy of pre-rendered blobs with no loop, branch or slot; fragment-heavy rendered one `tile` partial 48 times — one shape, no dispatch, no nesting. **(1) Composition mechanism — NO engine change.** The language already sanctions the shape (language-reference §"Composition without coupling"): the layout lives INSIDE a definition — `<layout>{{ …chrome… @out() … }} :: ComposedModel` in a definition-only file that renders nothing at import — and the page does `@<<{{layout.heddle}}` + `@layout(){{ …body… }}`, the caller body splicing at the live `@out()` slot at zero render-time cost. Proven at benchmark scale (multi-KB literal chrome, section-default overrides, regions through imports, FullCSharp, both backends) by `src/Heddle.Tests/LayoutDefinitionCompositionTests.cs`, which also pins the two chrome-authoring hazards: an adjacent `}}` in minified CSS is the subtemplate close token (compile fails), and literal `@` must be escaped `@@`. D5's premise — "changing the shape requires an engine change" — is thereby retired: the engine always could; the templates never used the idiom. **(2) Hybrid data model.** Four inert blob areas stay in `AreaData` (Alert Top, Secondary Wholesale/Retail Menus, pinned-empty Alert Below); the two ~21 KB mega menus and the footer links are removed from it and re-expressed as structured `NavData` (`NavModel { Menus, FooterColumns }` → `MegaMenu { Tabs }` → `MenuTab { Label, Href, Css, HasDropdown, DropdownCss, Columns }` → `NavColumn { Sections }` → `NavSection { Title, Href, TitleLinked, Links }` → `NavLink { Label, Href }`), rendered by `@list` loops over nested `mega_menu`/`nav_column`/`nav_section`/`nav_link` definitions. Every nav text value is sanitized to workloads.md rule 4 (`&`→`and`; apostrophes, ™ and accents dropped) — a blocking pre-decision for every port, since default-escaping engines would otherwise double-escape — and `NavData`'s static constructor asserts the rule. `export-corpus` additionally exports **`GoldenCorpus/fixtures/composed-page/nav.json`** (snake_case, LF, stable order), recorded in a new manifest `fixtures` section with golden-grade hash+length and freshness-verified by `verify-corpus`; the five port ecosystems load their nav models from it. Both tracks' `home.heddle` carry the SAME slider body (idiomatic was empty — that ends), and the verifier's removed-segment calibration pin is the slider, so an empty body fails. **(3) fragment-heavy.** 48 rows, `Kind = {tile,card,media,stat}[i%4]` (12 each), precomputed `IsTile/IsCard/IsMedia/IsStat` booleans (common-denominator dispatch), a per-kind `Delta` int, and `FragmentPromo Promo { Label, Price }` (Price an int) on every row — the model carries DATA only; derived display text (media caption `Caption for `+name, image src `/img/`+name+`.jpg`, display price +`.99`) is composed by the templates as literal-plus-substitution, since pre-formatting it model-side would move rendering work out of the engine under test; six Heddle definitions (`tile`, `card` — which nests `@badge(Promo)` + `@price(Promo)`, the one nesting level — `badge`, `price`, `media_row`, `stat`) dispatched per row by an `@if/@elif/@elif/@else` chain. **Consequences, measured at this entry's export:** composed-page golden 41,111 B stored (raw render 47,455 chars — 94,910 B as UTF-16, still above the LOH line; 47,459 B as UTF-8), fragment-heavy 6,057 B; N3b selftest pins re-pinned to 39,481 / 5,840 (to be re-confirmed against the JS reference when the ports land); `VerifierDefinitions.ComposedPage()`/`FragmentHeavy()` rewritten with model-computed needles; the selftest's 17-fragment-concatenation premise replaced by blob-occurrence + an independent C# NavData expansion + per-link checks; manifest hashes for rows 1 and 6 re-recorded at the corpus regeneration (all six other goldens byte-identical, verified). **Precompiled coverage, discovered not assumed:** RAISED to 6/8 — the redesign removed the last embedded C# from the benchmark templates (the extension arguments became native string literals, `@area_component("…")`, and composed-page's compile spec moved from `FullCSharp` to `Native` accordingly), so `home.heddle`'s two historical refusals (embedded C# outside FullCSharp; `<body:body>` full-override) are both gone and the emitter now covers every raw workload, byte-identical across all three sinks (`gate-precompiled`); only the two encoded workloads remain on the dynamic path (compilation-wide `HeddleOutputProfile`). **Ports:** every twin and ecosystem renders composed-page through its OWN native layout mechanism with a live body slot and fragment-heavy through its native dispatch chain (normative per-engine tables in workloads.md); their cells are expectedly red between the Heddle-side landing and the port landing. Also added: idiomatic `mixed-page` is single-file — layout composition is composed-page's dimension. | Benchmark workload redesign (composed-page / fragment-heavy), 2026-08-08 | [phase-1 README D5](cross-stack-benchmarks/phase-1-cross-stack-foundation/README.md#d5--the-composed-page-anchor-keeps-its-fragment-sequence-shape-q14) (superseded); [workloads.md](cross-stack-benchmarks/phase-1-cross-stack-foundation/workloads.md) anchors freeze + workloads 1 & 6 + size table + mixed-page idiomatic rule; [golden-corpus.md](cross-stack-benchmarks/phase-1-cross-stack-foundation/golden-corpus.md) layout/manifest/export/verification/verifier rows; [parity-contract-v2](cross-stack-benchmarks/phase-1-cross-stack-foundation/parity-contract-v2.md) existing-artifacts table; `benchmarks/dotnet/GoldenCorpus/README.md` (fidelity note + root cause deleted, full-page note added) |
| **E21 (NORMATIVE; extends E20's data-not-display rule to the whole suite).** **Maintainer ruling, 2026-08-08: the model-preparation tier carries DATA only — every derived display string is composed by the templates.** E20 applied the rule to the redesigned workloads; this entry applies it to the floors. Removed from the models and re-expressed as template-side literal-plus-substitution (portable to all 16 engines — no arithmetic, padding, or formatting helpers needed): `LoopContent.Name` (`"row-" + i`) — the large-loop template renders `row-@(Value)`; `MixedContent.Sku` (`$"MX-{1000+i}"`) → `SkuNumber = 1000 + i` int with the template composing `MX-@(SkuNumber)`; `MixedContent.Blurb` (a sentence with `{i}` spliced mid-text) → `Batch = i` int with the sentence living in the template; `ConditionalContent.Note` (`$"note {i}"`) → `Seq = i` int with the template composing `note @(Seq)`. Two categories stay model-side BY DESIGN and are not defects: **zero-padded identity names** (`item-{i:D2}`, `unit-{i:D3}`, `Product {i:D2}`) — row identity, not display text, and not composable within the common-denominator ceiling (vanilla Handlebars has no padding or arithmetic); and **encoded-suite payloads** (`tag-{i}&'{i%7}'`, the TechEmpower fortunes) — untrusted INPUT is the escaping subject, so it must arrive as model data. Golden bytes are unchanged (verify-corpus green against the pre-existing corpus); dictionary views rename `name→(dropped)`, `note→seq`, `sku→sku_number`, `blurb→batch`; every twin and port updates its large-loop / mixed-page / conditional-heavy templates to the composed forms (their cells are expectedly red until the port lands, same window as E20). The principle, stated once: pre-formatting display text in the model moves rendering work out of the engine under test. | Model/display separation suite-wide, 2026-08-08 | [workloads.md](cross-stack-benchmarks/phase-1-cross-stack-foundation/workloads.md) workloads 3–5 model shapes + normative template texts (E21 notes inline) |
| **E22 (NORMATIVE; completes E21's rule — supersedes E20's hybrid blob/structured split).** **Maintainer directive, 2026-08-08: NO text blobs in the C# tier at all — templates hold ALL formatting and ALL text; the C# tier holds only model DATA.** E20 kept four inert blob areas and six extension-served fragments in C# (`AreaData`, `TwinContent.Comp*`, the `[ExportExtensions]` components); E21 established the principle for derived display strings; this entry finishes the job for literal text. **What moved:** the four blob areas (alert banner, both secondary menus, the pinned-empty alert-below slot) and the six fixed asset/script fragments became named definitions in a new definition-only fragment library, `templates/{track}/heddle/chrome-fragments.heddle`, imported by `layout.heddle` via `@<<` (nested imports are the documented mechanism, pinned by Stage-1 scenario (f)); the chrome calls them at its composition points — `@alert_top()`, `@secondary_wholesale_menu()`, `@secondary_retail_menu()`, `@alert_below()`, `@assets_styles()`, `@assets_scripts()`, and the `@custom_styles()`/`@head_scripts()`/`@body_scripts()`/`@body_end_scripts()` call sites kept their spelling, resolving to definitions where they used to resolve to extensions. Definitions over inline chrome, deliberately: they preserve the composed structure, keep every fragment page-overridable with the ordinary `<name:name>` mechanism, and keep `layout.heddle` from inflating. **What is deleted:** `HeddleExtensions/Components.cs` with its `[assembly: ExportExtensions]` (with no C# text to serve, the extensions had no purpose — and the twins never had extensions, so composed-page is now pure template composition on every engine), `AreaData.cs`, and `TwinContent.cs` whole (its remaining purpose was serving text fixtures to twins; the twin engine modules now pass only the nav model view). `ComposedModel` stays exactly `{ Nav }`; `fixtures/composed-page/nav.json` is unchanged and remains the only composed-page data fixture. **Hazards, checked before the move:** the moved text contains no `@`, no `{{` and no `}}` — no `@@` escaping was needed anywhere. **Razor needed no workaround** — literal chrome is Razor-native. **Evidence the bytes did not move:** all sixteen corpus artifacts plus nav.json re-export byte-identical, `verify-corpus` green against the committed corpus with NO re-export, selftest 145/145 (down one by design: the blob-once-in-order model check lost its model side, and chrome presence/order is exactly what the verifier's ordered markers already prove; the independent NavData expansion and per-link checks stay), gate-precompiled unchanged at 6/8 — the fragment library and the nested import are emitted. **Consequence for every port:** the inert chrome is literal template text transcribed from the Heddle templates/golden, policed by the byte gate (controlled) and verifier (idiomatic); the per-ecosystem blob fixture files and their hash-freshness tests are deleted when each port lands. | Maintainer directive (no text in the C# tier), 2026-08-08 | [E20](#cross-spec-amendments-ledger)'s hybrid blob/structured split (superseded); [workloads.md](cross-stack-benchmarks/phase-1-cross-stack-foundation/workloads.md) workload 1 (model, template shape, mechanism-table note); [golden-corpus.md](cross-stack-benchmarks/phase-1-cross-stack-foundation/golden-corpus.md) composed-page verifier row + stored-form note; `benchmarks/dotnet/GoldenCorpus/README.md` full-page note; the benchmark csproj's precompile-coverage comment; 13 phase 1–6 citations of the deleted `TwinContent.cs`/`AreaData.cs` de-linked with their visible text preserved ([E15](#cross-spec-amendments-ledger) precedent — the prose still names what it cited; the artifact behind it is gone) |
| **E23 (RECORD; the E20–E22 port landing — no rule changed, facts recorded).** **All fifteen engine ports landed 2026-08-08, same-day as E20–E22** (five .NET twins — Fluid, Scriban, DotLiquid, Handlebars, Razor — and ten ecosystem engines — Askama, Tera, JTE, Thymeleaf, eta, handlebars, Jinja2, Mako, Go stdlib, templ), each rebuilding composed-page on its own native layout mechanism with a live body slot and fragment-heavy on four dispatched kinds with nested badge + price, the E21 floors on template-composed display text, and all chrome as literal template text (E22). **Executed probe results:** the Handlebars partial-block layout (`{{#> layout}}` + `{{> @partial-block}}`) is supported with exact bytes on **Handlebars.Net 2.1.6** and **handlebars 4.7.9** (both compile paths) — the documented registered-body-partial fallback was not needed. **Thymeleaf passed the controlled byte gate with NO exclusion invoked** — the standing exclusion-with-evidence fallback stayed unused; two new authoring classes entered its feasibility ladder: **B10** (fragment-selector tag-name collision — a `~{:: name}` selector also matches elements named `name`) and **B2b** (`th:attr` appends generated attributes at the end of the tag). **templ gained finding F7** (the generator normalizes self-closing void elements to slashless; the oracle's nine composed-page ` />` sites + the fragment media `<img … />` emit exact bytes via `@templ.Raw` over pinned literals — the doctype-accommodation mechanism). **One engine-forced name deviation suite-wide:** Thymeleaf's `meta` section fragment is `meta_section` (B10). Also landed: the dead un-suffixed Liquid sources (`composed-page.liquid`, `layout.liquid`, `large-loop.liquid`, …) deleted in favor of the dialect-suffixed `.fluid.liquid`/`.dotliquid.liquid` pairs; `consolidate.py`'s `RENDERED_CHARS` re-pinned to the redesigned sizes (composed-page **47,455**, fragment-heavy **6,058**). **Gates at the landing commit, all green:** .NET 146/146, Rust 32/32 (+32 lib tests), JVM 32/32, JS 32/32 (+82 selftest), Python 32/32 (+133 selftest), Go suites green; selftest 145/145, corpus verified, goldens untouched. | Port landing wave + docs reconciliation, 2026-08-08 | [workloads.md](cross-stack-benchmarks/phase-1-cross-stack-foundation/workloads.md) workload-1 mechanism table (probe/fallback clauses discharged) + workload-6 dispatch table (landed constructs; controlled-boolean vs idiomatic-native-switch stated); [phase-2 workload-ports.md](cross-stack-benchmarks/phase-2-rust/workload-ports.md) workload 1/3/4/5/6 template sections (rewritten to landed forms); [phase-3 construct-mapping.md](cross-stack-benchmarks/phase-3-jvm/construct-mapping.md) matrix rows 1 & 6 + normative texts; [phase-3 harness-and-jmh.md](cross-stack-benchmarks/phase-3-jvm/harness-and-jmh.md) source tree (`composed-page/*.txt` deleted); [phase-4 templates-and-models.md](cross-stack-benchmarks/phase-4-js/templates-and-models.md) helper surface (the `area` helper deleted — zero registered helpers; the knownHelpers divergence disclosure superseded) + eta/handlebars sections; [phase-5 templates.md](cross-stack-benchmarks/phase-5-python/templates.md) jinja2/mako sections; [phase-6 port-mapping.md](cross-stack-benchmarks/phase-6-go/port-mapping.md) stdlib workload 1 + templ rules 5/8; [phase-6 templ-feasibility.md](cross-stack-benchmarks/phase-6-go/templ-feasibility.md) F7 + F4 note |
